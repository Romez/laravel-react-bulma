const prefix = 'CATEGORIES--'

export default {
  APPEND_GOODS: `${prefix}--APPEND_GOODS`,
  REVERT_STATE: `${prefix}--REVERT_STATE`,
  SKIP_GOODS: `${prefix}--SKIP_GOODS`,
  UPDATE_TOTAL_CATEGORY_GOODS: `${prefix}--UPDATE_TOTAL_CATEGORY_GOODS`,
  UPDATE_GOODS_PER_PAGE: `${prefix}--UPDATE_GOODS_PER_PAGE`,
  UPDATE_GOODS_LOADING: `${prefix}--UPDATE_GOODS_LOADING`
}
